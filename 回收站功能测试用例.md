# 回收站功能测试用例

## 运行方式

```bash
npm test
```

测试脚本：`src/tests/recycleBin.test.js`

## 前置条件

- MySQL 服务可用。
- `DB_PASSWORD` 已配置为本机数据库密码。
- 测试会自动创建并清理手机号 `13900000001` 到 `13900000004` 的临时用户、测试商品和测试订单。

## 测试范围

| 编号 | 场景 | 操作 | 预期结果 |
| --- | --- | --- | --- |
| TC-RB-001 | 删除有关联订单和商品的用户 | 调用用户软删除 | 用户进入回收站，原手机号写入 `deleted_phone`，当前 `phone` 被替换，关联未删除商品进入商品回收站 |
| TC-RB-002 | 删除用户后手机号复用 | 用原手机号创建新用户 | 注册可成功，不被旧回收站用户占用 |
| TC-RB-003 | 手机号冲突时恢复旧用户 | 原手机号已被新用户占用时恢复旧用户 | 恢复失败，返回 `phone_conflict`，避免两个账号共用手机号 |
| TC-RB-004 | 彻底删除用户 | 对回收站用户执行彻底删除 | 用户、其商品、关联订单被物理删除，不再受外键限制 |
| TC-RB-005 | 恢复用户后登录 | 对无手机号冲突的回收站用户执行恢复，再用原手机号密码登录 | 恢复成功，登录成功并进入 `/products` |
| TC-RB-006 | 商品单独删除与恢复 | 对商品执行软删除再恢复 | 商品状态恢复为原状态，`deleted_at` 清空 |
| TC-RB-007 | 商品彻底删除 | 对回收站商品执行彻底删除 | 商品被物理删除，关联订单同步清理 |

## 最近一次验证结果

- `npm test`：通过。
- 模块加载检查：通过。
- EJS 模板编译检查：通过。
- 管理后台页面渲染检查：首页、用户管理、商品管理、回收站均通过。
- HTTP 验证：管理员登录后可访问 `/admin/recycle-bin`。
